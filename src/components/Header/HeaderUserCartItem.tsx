import { Product } from '@chec/commerce.js/types/product';

export default function HeaderUserCartItem({
  name,
  image,
}: Product): JSX.Element {
  return <div className="header-user-cart-item"></div>;
}
